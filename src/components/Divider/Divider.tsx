import { Box } from "@mui/material";

const Divider = () => {
  return (
    <Box
      sx={{ border: ".1rem solid #fff", margin: "0 5rem 0" }}
      className="animate__animated animate__fadeInDown navbar"
    />
  );
};

export default Divider;
